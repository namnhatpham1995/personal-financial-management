package com.fintrack.common.domain;

/** Shared transaction type enum used by transactions, categories, and recurring definitions. */
public enum TransactionType {
    INCOME,
    EXPENSE,
    TRANSFER
}
